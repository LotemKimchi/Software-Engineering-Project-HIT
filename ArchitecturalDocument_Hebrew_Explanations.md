# TaskMaster Pro — הסברים בעברית למסמך עיצוב ארכיטקטורי

---

## 1. דיאגרמות פעילות (Activity Diagrams)

---

### תהליך 1: הרשמה וכניסה למערכת

**תיאור התהליך:**

תהליך זה מתאר את שני מסלולי הכניסה הראשיים של המשתמש למערכת TaskMaster Pro: הרשמה כמשתמש חדש וכניסה כמשתמש קיים.

**מסלול הרשמה (משתמש חדש):**
המשתמש בוחר באפשרות "הרשמה" מהתפריט הראשי. המערכת מבקשת שם משתמש, סיסמה ותחום מקצועי. המערכת מאמתת כי שם המשתמש ייחודי וכי הסיסמה כוללת לפחות 8 תווים. אם הנתונים תקינים, הסיסמה עוברת גיבוב (hashing) ונשמרת בקובץ מקומי, ומוצגת הודעת הצלחה. אם הנתונים אינם תקינים, מוצגת הודעת שגיאה מתאימה.

**מסלול כניסה (משתמש קיים):**
המשתמש מזין שם משתמש וסיסמה. המערכת בודקת את הפרטים מול קובץ הנתונים המקומי. אם הפרטים נכונים, מונה ניסיונות הכשל מתאפס, נוצרת סשן פעיל, ומשימות המשתמש נטענות לפי תחום מקצועי. אם הסיסמה שגויה, הודעת שגיאה מוצגת ומונה הכשלים עולה ב-1. לאחר 3 ניסיונות כושלים, החשבון נעול למשך 15 דקות כדי להגן מפני מתקפות כוח גס.

**קישור לדרישות:**
תהליך זה ממומש דרישות פונקציונליות FR01 (כניסה), FR02 (הרשמה), ודרישת אבטחה NFR-SEC (נעילת חשבון אחרי 3 ניסיונות כושלים).

---

### תהליך 2: ניהול מחזור חיים של משימה

**תיאור התהליך:**

תהליך זה מתאר כיצד משתמש מנהל משימה קיימת במערכת: עדכון פרטים, שינוי סטטוס, ומחיקה.

**עריכת משימה:**
המשתמש בוחר משימה מהרשימה ובוחר בפעולת "עריכה". המערכת בודקת אם הסטטוס הוא "סגור" (Done) — אם כן, פעולת העריכה נחסמת ומוצגת הודעת שגיאה. אחרת, המשתמש יכול לשנות כותרת, תיאור ועדיפות, והמידע נשמר מיד לקובץ.

**עדכון סטטוס:**
מחזור החיים של הסטטוס הוא חד-כיווני בלבד: חדש (New) → בתהליך (In Progress) → סגור (Done). לא ניתן להחזיר סטטוס לאחור. ניסיון לעדכן משימה שכבר סגורה מוביל להצגת הודעה מתאימה.

**מחיקת משימה:**
המשתמש מבקש מחיקה ומוצג חלון אישור. רק לאחר אישור מפורש, המשימה נמחקת מקובץ הנתונים ומוצגת הודעת הצלחה. ביטול הפעולה מחזיר את המשתמש לרשימה ללא שינוי.

**קישור לדרישות:**
תהליך זה ממומש דרישות פונקציונליות FR03 (עדכון משימה), FR05 (מחיקת משימה), ו-FR04 (שינוי סטטוס). ה-NFR שדורש שמירה מיידית לאחר כל פעולה ממומש על ידי הקריאה ל-FileStorage לאחר כל שינוי.

---

### תהליך 3: יצירת משימה / באג

**תיאור התהליך:**

תהליך זה מתאר כיצד משתמש מחובר יוצר משימה חדשה (Task) או באג (Bug) במערכת.

**זרימת התהליך:**
המשתמש מזין כותרת (עד 50 תווים), תיאור מפורט, סוג (משימה/באג), עדיפות (נמוכה/בינונית/גבוהה) ותאריך יעד. אם סוג הפריט הוא "באג", שדה "שלבי שחזור" (Steps to Reproduce) הופך לשדה חובה. תאריך היעד מאומת — תאריך עבר אינו מתקבל.

**לאחר אימות מוצלח:**
המשתמש מוקצה אוטומטית לתחום המקצועי שלו. נוצר מזהה ייחודי למשימה. הסטטוס ההתחלתי נקבע כ-"חדש" (New). המשימה נשמרת מיד לקובץ המקומי ומוצגת הודעת אישור הכוללת את מזהה המשימה שנוצר.

**טיפול בשגיאות:**
כל שגיאת אימות מוצגת בנפרד ומפורש למשתמש (כגון: "שדה חובה חסר", "תאריך בעבר").

**קישור לדרישות:**
תהליך זה ממומש דרישות FR01 (יצירת משימה), FR06 (שדה חובה לבאג), ו-NFR4 (שמירה מיידית לאחר יצירה).

---

## 2. דיאגרמות רצף (Sequence Diagrams)

---

### UC1: כניסת משתמש (User Login)

**תיאור תרחיש השימוש:**

תרחיש שימוש זה מתאר את זרימת הכניסה של משתמש קיים למערכת, תוך הצגת האינטראקציות בין כל רכיבי המערכת.

**זרימה רגילה (Happy Path):**
המשתמש מזין שם משתמש וסיסמה ב-UIController. ה-UIController מעביר את הפרטים ל-AuthService דרך קריאה ל-authenticate(). ה-AuthService מאחזר את רשומת המשתמש דרך UserRepository, אשר קורא מקובץ הנתונים המקומי. לאחר אחזור מוצלח, AuthService מאמת את הסיסמה מול הגיבוב השמור. אם הסיסמה נכונה, מאופס מונה הכשלים, נוצרת Session חדשה, ומוחזר תוצאת SUCCESS ל-UIController, אשר מציג את רשימת המשימות לפי התחום המקצועי.

**זרימות חלופיות (Alternative Flows):**
- **משתמש לא נמצא:** AuthService מחזיר USER_NOT_FOUND, ומוצגת הודעת שגיאה.
- **סיסמה שגויה (פחות מ-3 ניסיונות):** מונה הכשלים עולה ב-1, ומוצגת הודעת שגיאה עם מספר הניסיונות שנותרו.
- **נעילת חשבון (3 ניסיונות כושלים):** AuthService קורא ל-UserRepository לנעול את החשבון ל-15 דקות, ומוחזר ACCOUNT_LOCKED.

**רכיבים מעורבים:** UIController, AuthService, UserRepository, FileStorage (DB), Session.

---

### UC2: יצירת משימה / באג (Create Task / Bug)

**תיאור תרחיש השימוש:**

תרחיש שימוש זה מתאר את זרימת יצירת משימה חדשה, כולל אימות, הקצאת מזהה ושמירה לקובץ.

**זרימה רגילה (Happy Path):**
המשתמש בוחר "יצירת משימה חדשה" ו-UIController מציג את טופס הקלט. לאחר שהמשתמש מגיש את פרטי המשימה, UIController קורא ל-Validator לאמת את הכותרת ואת תאריך היעד. אם האימות עובר בהצלחה, UIController קורא ל-TaskService.createTask(). ה-TaskService מייצר מזהה ייחודי, מקצה תחום מקצועי ומגדיר סטטוס "חדש". לאחר מכן שולח את המשימה ל-TaskRepository.save(), שכותב לקובץ הנתונים.

**טיפול בשגיאת כתיבה (Retry Mechanism):**
אם כתיבת הקובץ נכשלת, TaskRepository מנסה מחדש עד 3 פעמים. אם כל הניסיונות נכשלים, מוחזרת SaveException והמשתמש רואה הודעת שגיאה ברורה.

**ייחודיות מסוג "באג":**
אם סוג המשימה הוא Bug, UIController מציג שדה נוסף לשלבי שחזור (Steps to Reproduce) לפני קריאה ל-TaskService.

**רכיבים מעורבים:** UIController, Validator, TaskService, TaskRepository, FileStorage (DB).

---

### UC3: חיפוש וסינון משימות (Search and Filter Tasks)

**תיאור תרחיש השימוש:**

תרחיש שימוש זה מתאר שלוש פעולות שונות שמשתמש יכול לבצע על רשימת המשימות: חיפוש טקסטואלי, סינון לפי סטטוס, ומיון לפי תאריך.

**חיפוש טקסטואלי:**
המשתמש מזין מחרוזת חיפוש. UIController קורא ל-TaskService.searchTasks(). ה-TaskService מאחזר את כל משימות התחום המקצועי של המשתמש מ-TaskRepository, ומעביר אותן ל-SearchFilter.filterByText(), שמבצע התאמה בכותרת ובתיאור.

**סינון לפי סטטוס "In Progress":**
UIController קורא ל-TaskService.filterByStatus(IN_PROGRESS). ה-TaskService מאחזר משימות לפי תחום ומעביר ל-SearchFilter.filterByStatus() לקבלת המשימות הרלוונטיות בלבד.

**מיון לפי תאריך יעד:**
UIController קורא ל-TaskService.sortByDueDate(). ה-TaskService מאחזר משימות ומעביר ל-SearchFilter.sortAscendingByDueDate() לקבלת רשימה ממוינת כרונולוגית.

**טיפול במקרי קצה:**
אם אין תוצאות, UIController מציג הודעה "לא נמצאו משימות". ברירת המחדל של מיון התצוגה היא לפי עדיפות.

**רכיבים מעורבים:** UIController, TaskService, TaskRepository, FileStorage (DB), SearchFilter.

---

## 3. דיאגרמת מחלקות (Class Diagram)

**תיאור הדיאגרמה:**

דיאגרמת המחלקות של TaskMaster Pro מציגה את מבנה האובייקטים של המערכת, מחולקת ל-5 חבילות (packages) המשקפות את ארכיטקטורת השכבות:

**שכבת המודל (Model Layer):**
- **Task:** המחלקה המרכזית של המערכת. מכילה מזהה, כותרת, תיאור, עדיפות, סטטוס, תאריך יעד, תחום מקצועי ומידע על יוצר המשימה. כוללת מתודות לניהול מחזור החיים של המשימה.
- **Bug:** יורשת מ-Task ומוסיפה שדה `stepsToReproduce` שהוא שדה חובה עבור כל באג.
- **User:** מייצגת משתמש במערכת. מכילה פרטי אימות (שם משתמש, גיבוב סיסמה), תחום מקצועי, מונה ניסיונות כושלים, וזמן נעילת חשבון.
- **Enum Priority / Status / TaskType:** מגדירים ערכים קבועים עבור עדיפות (LOW/MEDIUM/HIGH), סטטוס (NEW/IN_PROGRESS/DONE) וסוג (TASK/BUG).

**שכבת השירות (Service Layer):**
- **AuthService:** מטפל בהרשמה, כניסה, וניהול סטטוס חשבון. מבצע גיבוב סיסמאות ואימות.
- **TaskService:** מיישם את כל חוקי העסקים הקשורים למשימות: יצירה, עדכון, מחיקה, ושינוי סטטוס.
- **DashboardService:** מספק סטטיסטיקות על משימות פתוחות, בתהליך ומשימות דחופות.
- **ReportExporter:** מייצא דוחות משימות לקובץ טקסט.
- **Validator:** מבצע אימות קלט (כותרת, תאריך, סיסמה, שם משתמש).
- **SearchFilter:** מבצע חיפוש, סינון ומיון על רשימות משימות.

**שכבת הגישה לנתונים (Repository Layer):**
- **UserRepository:** אחראי על CRUD operations עבור משתמשים.
- **TaskRepository:** אחראי על CRUD operations עבור משימות, כולל יצירת מזהים ייחודיים.

**שכבת האחסון (Storage Layer):**
- **FileStorage:** מבצע קריאה וכתיבה לקובץ הנתונים המקומי. כולל מנגנון Retry — ניסיון חוזר עד 3 פעמים במקרה של כשל כתיבה.

**שכבת ממשק המשתמש (UI Layer):**
- **UIController:** שולט בתפריטים, קלט המשתמש, ותצוגת רשימות ושגיאות.
- **Session:** שומרת את נתוני המשתמש המחובר כגון העדפות תצוגה ומסנני ברירת מחדל.

**קשרים עיקריים:**
- Bug יורשת (inheritance) מ-Task — כל באג הוא גם Task, עם תוספת שדה.
- Task מכיל (composition) ערכי Enum: Priority, Status, TaskType.
- UIController מחזיק (association) שירותים: AuthService, TaskService, DashboardService.
- שירותים מחזיקים Repositories, ו-Repositories מחזיקים FileStorage.

---

## 4. תבנית ארכיטקטורה (Architectural Pattern)

### ארכיטקטורת שכבות (Layered Architecture)

**תיאור התבנית:**

המערכת TaskMaster Pro מיושמת באמצעות **ארכיטקטורת שכבות** (Layered Architecture), המחולקת לארבע שכבות ברורות ומופרדות:

| שכבה | רכיבים | אחריות |
|------|---------|---------|
| **שכבת ממשק המשתמש (UI)** | UIController, Session | קבלת קלט, הצגת תפריטים ותוצאות |
| **שכבת לוגיקה עסקית (Service)** | AuthService, TaskService, DashboardService, ReportExporter, Validator, SearchFilter | יישום כל חוקי העסקים |
| **שכבת גישה לנתונים (Repository)** | UserRepository, TaskRepository | בידוד ה-Service מפרטי האחסון |
| **שכבת אחסון (Storage)** | FileStorage, Local Data File | קריאה/כתיבה לקובץ מקומי עם Retry |

**עקרון הזרימה החד-כיוונית:**
כל שכבה מתקשרת **רק עם השכבה שמתחתיה** — UIController פונה ל-Service, Service פונה ל-Repository, ו-Repository פונה ל-FileStorage. אין קריאה הפוכה ואין דילוג על שכבות.

**הסבר לבחירת התבנית:**

1. **מערכת CLI, לא GUI:** TaskMaster Pro מבוסס על תפריטים ממוספרים בממשק שורת פקודה. תבנית MVC, שמתאימה לממשקים גרפיים עם אירועים, אינה מתאימה לזרימה הלינארית של הקלט.

2. **הפרדה מוחלטת כדרישה:** דרישה לא-פונקציונלית מספר 8 דורשת הפרדה מוחלטת בין שכבת הלוגיקה לשכבת הנתונים. ארכיטקטורת שכבות אוכפת הפרדה זו על ידי עיצוב.

3. **FileStorage כשכבה עצמאית:** מנגנון ה-Retry ועיצוב FileStorage מצדיקים הפרדתו לשכבה נפרדת, מה שאינו מתאים לתבנית Repository של MVC הקלאסי.

4. **זרימת נתונים לינארית:** הנתונים זורמים בכיוון אחד: משתמש → UIController → Service → Repository → FileStorage. ארכיטקטורת שכבות ממדלת זרימה זו ישירות.

5. **תחזוקה ובדיקות:** כל שכבה ניתנת לבדיקה בנפרד ולהחלפה ללא השפעה על השכבות האחרות.
